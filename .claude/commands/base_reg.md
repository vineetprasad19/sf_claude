Build an optimized results by utilizing less tokens and agents: $ARGUMENTS

- check existing custom objects and fields in the current project to use correct API namess
- Include field-level security considerations
- Add comments explaning any filters or relationships
- Flag if any fields use  AI_* or Coder_* prefixes
- suggest indexes if query large data volumes